{
  "name": "waqful-madinah",
  "private": true,
  "scripts": {
    "build": "node scripts/inject-supabase-config.mjs && node scripts/inject-pwa-config.mjs && node scripts/build-routes.mjs",
    "start": "npm run build && vercel dev --listen 3000",
    "import-firestore-students": "node scripts/import-firestore-students.mjs",
    "import-firestore-messages": "node scripts/import-firestore-messages.mjs"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.97",
    "@supabase/supabase-js": "^2.103.0",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^5.6.205",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "pg": "^8.20.0",
    "vercel": "^54.20.1"
  }
}
