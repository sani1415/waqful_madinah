# ── Dependencies ────────────────────────────────────────────
node_modules/

# ── Local env (copy from .env.example) ───────────────────────
.env
.env.local
.env.*.local

# ── Build-generated config (Vercel injects these at build time) ──
supabase-config.js
pwa-config.js
teacher/
student/

# ── Supabase secrets ─────────────────────────────────────────
supabase/.secrets.local.env
supabase/.secrets.vapid.env
supabase/.temp/

# ── Firestore migration / one-time import files ──────────────
firestore-export.json
tmp-firestore-*.sql
tmp-*.sql
tmp-*.json
tmp-*.txt



# ── AI session / editor internals ────────────────────────────
.claude/
.playwright-cli/
output/

# ── OS / editor noise ────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.log

.vercel
.env*
